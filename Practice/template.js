// Template string dynamic set up

const name = 'Jani na';
const bio = { name: 'Jani na', BF: 'Bipro', status: 'student', designation: 'jani na' }

const description = `My lover name is ${name}. She loves Bipro very much. She is now ${bio.designation} ${bio.status} . `;

//console.log(description);

const div = x => x / 5;
//console.log(div(30));


const sum = (x, y) => (x + 2) * (y + 2);
//console.log(sum(7, 10));

const mult = (x, y, z) => {

    gun = x * y * z;
    return gun;

}
console.log(mult(3, 4, 5));